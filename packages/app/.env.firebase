# Modo dev contra los emuladores de Firebase (pnpm -C packages/app dev:firebase).
# En prod, el build usa el proyecto real (Slice 2) con valores VITE_FIREBASE_* propios.
VITE_BACKEND=firebase
VITE_FIREBASE_EMULATOR=1
